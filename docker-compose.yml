version: '3.8'

services:
  ping:
    build: .
    container_name: ping-service
    ports:
      - "8080:8080"
    environment:
      - PORT=8080
      - APP_ENV=production
    restart: unless-stopped
